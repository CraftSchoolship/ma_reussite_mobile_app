import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: "80%",
    backgroundColor: "red",
    marginTop: "10%",
    paddingTop: "10%",
  },

  banner: {
    backgroundColor: "primary.500",
  },
});

export { styles };
